package ua.edu.lntu.quiz.data.enums

enum class AuthErrorType {
    NO_ERROR,
    INVALID_CREDENTIALS,
    USER_NOT_FOUND,
    EMAIL_ALREADY_IN_USE,
    ACCOUNT_EXISTS_WITH_DIFFERENT_CREDENTIAL,
    CREDENTIAL_ALREADY_IN_USE,
    WEAK_PASSWORD,
    EMAIL_NOT_VERIFIED,
    USER_DISABLED,
    USER_TOKEN_EXPIRED,
    INVALID_USER_TOKEN,
    REQUIRES_RECENT_LOGIN,
    MULTI_FACTOR_AUTH_REQUIRED,
    MISSING_ACTIVITY_FOR_RECAPTCHA,
    NETWORK_ERROR,
    WEB_CONTEXT_ALREADY_PRESENTED,
    WEB_CONTEXT_CANCELED,
    WEB_STORAGE_UNSUPPORTED,
    WEB_INTERNAL_ERROR,
    UNKNOWN_ERROR
}